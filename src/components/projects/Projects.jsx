import Navbar from "../navbar/Navbar";
import React from 'react';
import { Link } from "react-router-dom";

const  Projects = () => {
    return (
        // crear la base de datos para agregar los datos de los proyectos de manera dinamica, titulo, descripcion, nombre de la imagen. 
        <div></div>
    );
};
export default Projects;